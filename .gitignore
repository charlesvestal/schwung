.DS_Store

# Build artifacts
/build/
/dist/
/schwung.tar.gz
/libs/quickjs/quickjs-2025-04-26/.obj/
/libs/quickjs/quickjs-2025-04-26/libquickjs.a
/.worktrees
docs/.DS_Store
*.tar.gz

# Test patches (kept locally, not committed)
/src/modules/chain/test_patches/

# Build caches
build/cache/
/.cache/

# Binary files that shouldn't be committed
*.wav
*.asd
move_audio_recv
libs/memfault-firmware-sdk/

# Phase 0 recon — keep curated outputs under recon/ but skip the raw strace
# logs (large, redundant, unstable across passes).
recon/move-strace.log
recon/move-strace-*.log
