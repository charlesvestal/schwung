{
  "planPath": "docs/superpowers/plans/2026-08-23-component-trailing-pages.md",
  "tasks": [
    {
      "id": 0,
      "subject": "Task 1: Append trailing menus in the planner",
      "status": "pending",
      "description": "**Goal:** `planPages` appends caller-supplied menu pages after the whole walk, on resolved paths only.\n\n**Files:**\n- Modify: `src/shared/param_pages/page_plan.mjs:153` (signature), `:243` (fallback return), `:620` (main return)\n- Test: `tests/host/test_param_pages_menu.sh`\n\n**Acceptance Criteria:**\n- [ ] Trailing pages land LAST for an ordinary hierarchy with child levels\n- [ ] Trailing pages land last for the no-ui_hierarchy chain_params fallback\n- [ ] Trailing pages land last for a modes hierarchy and one with no root level\n- [ ] No trailing pages when trailingMenus is absent\n- [ ] No trailing pages when unresolved: true\n- [ ] Each appended page carries trailing:true and a name claimed through claimName\n\n**Verify:** bash tests/host/test_param_pages_menu.sh\n\n**Steps:**\nAdd the 7 planner cases (a)-(g) to the menu test; add the `trailingMenus` param and an `appendTrailing(pages, claim)` helper; call it at the chain_params fallback return and the main return; leave the two degenerate returns and the unresolved return alone.\n\n```json:metadata\n{\"files\": [\"src/shared/param_pages/page_plan.mjs\", \"tests/host/test_param_pages_menu.sh\"], \"verifyCommand\": \"bash tests/host/test_param_pages_menu.sh\", \"acceptanceCriteria\": [\"Trailing pages land LAST for an ordinary hierarchy with child levels\", \"Trailing pages land last for the no-ui_hierarchy chain_params fallback\", \"Trailing pages land last for a modes hierarchy and one with no root level\", \"No trailing pages when trailingMenus is absent\", \"No trailing pages when unresolved: true\", \"Each appended page carries trailing:true and a name claimed through claimName\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 1,
      "subject": "Task 2: Plumb trailingMenus through the controller",
      "status": "pending",
      "description": "**Goal:** The controller carries `trailingMenus` from its io to all three `planPages` sites, and can refresh just the trailing pages without moving the user.\n\n**Files:**\n- Modify: `src/shared/param_pages/page_controller.mjs:421` (io), `:763`, `:2130`, `:2148` (plan sites), `:2725` (exports)\n- Test: `tests/host/test_param_pages_menu.sh`\n\n**Acceptance Criteria:**\n- [ ] A controller built with io.trailingMenus plans trailing pages at all three plan sites\n- [ ] trailingMenus is a FUNCTION evaluated at plan time, so conditional rows track state\n- [ ] refreshTrailing() re-evaluates it and replaces only trailing pages\n- [ ] refreshTrailing() leaves pageIndex where it was, clamped if the page count shrank\n- [ ] A controller with no io.trailingMenus plans no trailing pages\n\n**Verify:** bash tests/host/test_param_pages_menu.sh\n\n**Steps:**\nAdd a `trailingMenus()` resolver next to the other io-derived helpers; pass `trailingMenus: trailingMenus()` at load, replanForMode and replanIfCondition; add `refreshTrailing()` that keeps non-trailing pages, re-appends, and clamps pageIndex; export it.\n\n```json:metadata\n{\"files\": [\"src/shared/param_pages/page_controller.mjs\", \"tests/host/test_param_pages_menu.sh\"], \"verifyCommand\": \"bash tests/host/test_param_pages_menu.sh\", \"acceptanceCriteria\": [\"A controller built with io.trailingMenus plans trailing pages at all three plan sites\", \"trailingMenus is a FUNCTION evaluated at plan time, so conditional rows track state\", \"refreshTrailing() re-evaluates it and replaces only trailing pages\", \"refreshTrailing() leaves pageIndex where it was, clamped if the page count shrank\", \"A controller with no io.trailingMenus plans no trailing pages\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        0
      ]
    },
    {
      "id": 2,
      "subject": "Task 3: Current-preset bookkeeping",
      "status": "pending",
      "description": "**Goal:** A pure module that hashes a state blob, decides whether the loaded preset is modified, and formats the row label.\n\n**Files:**\n- Create: `src/shared/param_pages/current_preset.mjs`\n- Test: `tests/host/test_current_preset.sh`\n\n**Acceptance Criteria:**\n- [ ] hashState is stable across calls and differs for differing blobs\n- [ ] hashState treats null (failed read) and \"\" (declares none) differently\n- [ ] presetRowValue renders (none), Name, and Name * for the three states\n- [ ] A record whose hash is null never reports modified\n- [ ] Module is pure: no ctx import, no host calls\n\n**Verify:** bash tests/host/test_current_preset.sh\n\n**Steps:**\nWrite the test first, then `hashState` (FNV-1a plus length, null in / null out), `makeRecord`, `isModified` (false whenever it cannot tell) and `presetRowValue`.\n\n```json:metadata\n{\"files\": [\"src/shared/param_pages/current_preset.mjs\", \"tests/host/test_current_preset.sh\"], \"verifyCommand\": \"bash tests/host/test_current_preset.sh\", \"acceptanceCriteria\": [\"hashState is stable across calls and differs for differing blobs\", \"hashState treats null (failed read) and \\\"\\\" (declares none) differently\", \"presetRowValue renders (none), Name, and Name * for the three states\", \"A record whose hash is null never reports modified\", \"Module is pure: no ctx import, no host calls\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 4: Persist the current-preset record in slot_N.json",
      "status": "pending",
      "description": "**Goal:** The loaded-preset record survives a reboot, riding the slot autosave that already carries state and bypass.\n\n**Files:**\n- Modify: `src/shadow/shadow_ui.js:6618` (`componentEntry`), and the matching slot-load path\n- Test: `tests/host/test_slot_patch_user_preset.sh`\n\n**Acceptance Criteria:**\n- [ ] componentEntry writes user_preset {name,hash} when a record exists\n- [ ] No user_preset key is written when no preset is loaded\n- [ ] The slot load path restores the record for each component prefix\n- [ ] A patch file written before this change loads with no record and no error\n- [ ] The existing BAIL guard is untouched\n\n**Verify:** bash tests/host/test_slot_patch_user_preset.sh\n\n**Steps:**\nAdd `currentUserPresets` keyed slot:prefix with get/set helpers; import the current_preset helpers; write `entry.user_preset` conditionally from componentEntry; restore it on the slot load path.\n\n```json:metadata\n{\"files\": [\"src/shadow/shadow_ui.js\", \"tests/host/test_slot_patch_user_preset.sh\"], \"verifyCommand\": \"bash tests/host/test_slot_patch_user_preset.sh\", \"acceptanceCriteria\": [\"componentEntry writes user_preset {name,hash} when a record exists\", \"No user_preset key is written when no preset is loaded\", \"The slot load path restores the record for each component prefix\", \"A patch file written before this change loads with no record and no error\", \"The existing BAIL guard is untouched\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        2
      ]
    },
    {
      "id": 4,
      "subject": "Task 5: Extract the picker apply path for reuse",
      "status": "pending",
      "description": "**Goal:** One function applies a chain-component pick, so \"Remove Module\" is literally the picker's `None` rather than a second copy of it.\n\n**Files:**\n- Modify: `src/shadow/shadow_ui.js:9380-9436` (extract from `applyComponentSelection`)\n- Test: `tests/host/test_chain_pick_single_source.sh`\n\n**Acceptance Criteria:**\n- [ ] applyChainComponentPick(slotIndex, componentKey, picked, pending) holds the whole sequence\n- [ ] applyComponentSelection calls it and does nothing else after resolving picked/pending\n- [ ] Called with picked=\"\" and pending=null it performs exactly the None removal including the remove shape verb\n- [ ] No second call to applyComponentSelectionConfirmed exists outside this function\n\n**Verify:** bash tests/host/test_chain_pick_single_source.sh\n\n**Steps:**\nMove the block from `const cfg = chainConfigs[selectedSlot]` through the final applyComponentSelectionConfirmed into applyChainComponentPick, substituting slotIndex/componentKey; call it from applyComponentSelection.\n\n```json:metadata\n{\"files\": [\"src/shadow/shadow_ui.js\", \"tests/host/test_chain_pick_single_source.sh\"], \"verifyCommand\": \"bash tests/host/test_chain_pick_single_source.sh\", \"acceptanceCriteria\": [\"applyChainComponentPick(slotIndex, componentKey, picked, pending) holds the whole sequence\", \"applyComponentSelection calls it and does nothing else after resolving picked/pending\", \"Called with picked=\\\"\\\" and pending=null it performs exactly the None removal including the remove shape verb\", \"No second call to applyComponentSelectionConfirmed exists outside this function\"], \"modelTier\": \"deep\"}\n```"
    },
    {
      "id": 5,
      "subject": "Task 6: The User Presets and Module pages",
      "status": "pending",
      "description": "**Goal:** Two trailing menu pages \u2014 the loaded preset (with `*`) plus Load\u2026/Save/Save As/Delete, and Swap/Remove \u2014 wired for real chain components only.\n\n**Files:**\n- Modify: `src/shadow/shadow_ui_presets.mjs`, `src/shadow/shadow_ui.js`, `src/shadow/shadow_ui_param_pages.mjs`, `tools/param-pages/preview.mjs`\n- Test: `tests/host/test_trailing_pages_wiring.sh`\n\n**Acceptance Criteria:**\n- [ ] Row 1 is informational: label Preset, value (none) / Name / Name *\n- [ ] Load\u2026 opens the existing browser screen; its mechanics are unchanged\n- [ ] Save overwrites the loaded preset in place and clears the *; absent when none is loaded\n- [ ] Save As is always present and goes to the keyboard\n- [ ] Delete targets the loaded preset; absent when none is loaded\n- [ ] The * is read on page entry and after our own writes, never on the draw path\n- [ ] Slot Settings, Master FX Settings and Master FX components get no trailing pages\n- [ ] The rendered PNGs were looked at, not just the text art\n\n**Verify:** bash tests/host/test_trailing_pages_wiring.sh\n\n**Steps:**\nCache the live state blob on entry; build componentTrailingMenus(slot, componentKey); pass it plus runAction on the component enterParamPages only; dispatch the six actions; add overwriteUserPreset / enterPresetSaveAs / enterPresetDeleteConfirm; keep the browser's Load/Save/Delete updating the record; add --trailing to preview.mjs and look at the PNGs.\n\n```json:metadata\n{\"files\": [\"src/shadow/shadow_ui.js\", \"src/shadow/shadow_ui_presets.mjs\", \"src/shadow/shadow_ui_param_pages.mjs\", \"tools/param-pages/preview.mjs\", \"tests/host/test_trailing_pages_wiring.sh\"], \"verifyCommand\": \"bash tests/host/test_trailing_pages_wiring.sh\", \"acceptanceCriteria\": [\"Row 1 is informational: label Preset, value (none) / Name / Name *\", \"Load\\u2026 opens the existing browser screen; its mechanics are unchanged\", \"Save overwrites the loaded preset in place and clears the *; absent when none is loaded\", \"Save As is always present and goes to the keyboard\", \"Delete targets the loaded preset; absent when none is loaded\", \"The * is read on page entry and after our own writes, never on the draw path\", \"Slot Settings, Master FX Settings and Master FX components get no trailing pages\", \"The rendered PNGs were looked at, not just the text art\"], \"modelTier\": \"deep\"}\n```",
      "blockedBy": [
        1,
        3,
        4
      ]
    },
    {
      "id": 6,
      "subject": "Task 7: One reconciler for grid actions",
      "status": "pending",
      "description": "**Goal:** The four grid-action handlers share one reconciler instead of four copies of the same reasoning.\n\n**Files:**\n- Modify: `src/shadow/shadow_ui.js:9631` and the three sibling handlers\n- Test: `tests/host/test_grid_action_reconcile.sh`\n\n**Acceptance Criteria:**\n- [ ] One helper decides \"did this action put something else on screen\", used by all four\n- [ ] The helper asks what is on screen; it does not enumerate which actions leave\n- [ ] The return-to-grid reconcile still fires for every way a modal can finish\n- [ ] All four call sites behave as before\n\n**Verify:** bash tests/host/test_grid_action_reconcile.sh\n\n**Steps:**\nExtract gridActionOpenedSomething(); point runSlot/runMasterFx/runGlobal/runComponent ActionFromGrid at it, keeping each one's own follow-on; leave maybeReturnToSlotGrid's reconcile shape alone.\n\n```json:metadata\n{\"files\": [\"src/shadow/shadow_ui.js\", \"tests/host/test_grid_action_reconcile.sh\"], \"verifyCommand\": \"bash tests/host/test_grid_action_reconcile.sh\", \"acceptanceCriteria\": [\"One helper decides \\\"did this action put something else on screen\\\", used by all four\", \"The helper asks what is on screen; it does not enumerate which actions leave\", \"The return-to-grid reconcile still fires for every way a modal can finish\", \"All four call sites behave as before\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        5
      ]
    },
    {
      "id": 7,
      "subject": "Task 8: Documentation",
      "status": "pending",
      "description": "**Goal:** The trailing pages and the `trailingMenus` option are documented where the release checklist says they must be.\n\n**Files:**\n- Modify: `CLAUDE.md`, `docs/MODULES.md`, `src/shared/help_content.json`, `../schwung-catalog-site/manual.html`\n\n**Acceptance Criteria:**\n- [ ] CLAUDE.md records that the pages are appended by the planner and why not by hierarchy injection\n- [ ] docs/MODULES.md states that module authors declare nothing to get them\n- [ ] docs/MODULES.md records that menu: on a level lands after that level's grids, not last\n- [ ] help_content.json has an entry reachable from the new pages\n- [ ] manual.html describes both pages for users\n\n**Verify:** command grep -c trailing CLAUDE.md docs/MODULES.md\n\n**Steps:**\nSlot the CLAUDE.md text into the User Presets and knob-grid-default sections; add the MODULES.md note under Level Fields; add the help entry; update the catalog-site manual and commit it in that repo separately.\n\n```json:metadata\n{\"files\": [\"CLAUDE.md\", \"docs/MODULES.md\", \"src/shared/help_content.json\"], \"verifyCommand\": \"command grep -c trailing CLAUDE.md docs/MODULES.md\", \"acceptanceCriteria\": [\"CLAUDE.md records that the pages are appended by the planner and why not by hierarchy injection\", \"docs/MODULES.md states that module authors declare nothing to get them\", \"docs/MODULES.md records that menu: on a level lands after that level's grids, not last\", \"help_content.json has an entry reachable from the new pages\", \"manual.html describes both pages for users\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        6
      ]
    },
    {
      "id": 8,
      "subject": "Task 9: Hardware verification",
      "status": "pending",
      "description": "**Goal:** The feature is confirmed on a real Move, across component types and hierarchy shapes.\n\n**Files:**\n- none (verification only)\n\n**Acceptance Criteria:**\n- [ ] Both pages appear last for a synth, an audio FX and a MIDI FX\n- [ ] Both are inert until clicked into; jogging past does not fire Remove Module\n- [ ] Save clears the *, a knob turn brings it back, Save clears it again\n- [ ] Save As creates and becomes current; Load makes the chosen one current; Delete removes it and the rows collapse\n- [ ] Swap and Remove behave identically to the Shift+Click path\n- [ ] Reboot preserves the current-preset mark\n- [ ] A module with no ui_hierarchy gets both pages after its paginated params\n- [ ] minijv gets them last in each mode\n- [ ] With param_view = List nothing new appears and Shift+Click still works\n- [ ] Slot Settings and Master FX Settings gain no pages\n- [ ] param_pages fps still reports draws approximately equal to ticks in the mid-50s\n\n**Verify:** device walkthrough with observations recorded per component type\n\n**Steps:**\nBuild and deploy with ./scripts/install.sh local --skip-modules --skip-confirmation; arm debug_log_on; walk every criterion recording what was seen; check the fps line; disarm the log; open the PR.\n\n```json:metadata\n{\"files\": [], \"verifyCommand\": \"device walkthrough with observations recorded per component type\", \"acceptanceCriteria\": [\"Both pages appear last for a synth, an audio FX and a MIDI FX\", \"Both are inert until clicked into; jogging past does not fire Remove Module\", \"Save clears the *, a knob turn brings it back, Save clears it again\", \"Save As creates and becomes current; Load makes the chosen one current; Delete removes it and the rows collapse\", \"Swap and Remove behave identically to the Shift+Click path\", \"Reboot preserves the current-preset mark\", \"A module with no ui_hierarchy gets both pages after its paginated params\", \"minijv gets them last in each mode\", \"With param_view = List nothing new appears and Shift+Click still works\", \"Slot Settings and Master FX Settings gain no pages\", \"param_pages fps still reports draws approximately equal to ticks in the mid-50s\"], \"modelTier\": \"deep\"}\n```",
      "blockedBy": [
        7
      ]
    }
  ],
  "lastUpdated": "2026-08-23"
}